default:
  image: node:22-alpine

stages:
  - deploy

create-pages:
  stage: deploy
  before_script:
    - npm install --no-audit --no-fund
  script:
    - npm run build
  pages:
    publish: dist
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'